"""Executable entry points.

Every script inserts ``src/`` into ``sys.path`` so the modules in
``models/`` / ``data/`` / ``evaluation/`` / ``scripts/`` can be imported
by their short names (e.g. ``from neural_models import ModelConfig``).
"""
